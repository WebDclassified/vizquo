# ---- Dependencies ----
node_modules/

# ---- WXT / Vite build output ----
.output/
.wxt/
dist/
coverage/
*.local

# ---- Environment & secrets (never commit real keys) ----
.env
.env.*
!.env.example
*.pem
*.key

# ---- Test artifacts ----
test-results/
playwright-report/
blob-report/

# ---- Logs ----
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- IDE / editor ----
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ---- Maintainer-only working briefs (kept locally, not part of the repo) ----
# The master requirements brief, the landing v2 design brief, and the daily
# handoff notes live in _extra/ on the maintainer's machine. They drove the
# implementation but are not project documentation — see README + CHANGELOG
# for the outcomes. The release pipeline tolerates their absence.
_extra/
